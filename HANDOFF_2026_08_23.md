# Third-Party Reconciliation - Handoff Prompt (2026-08-23)

Paste this whole file as the first message in the next chat window.

---

You are picking up the Third-Party Reconciliation production pipeline mid-flight. The **skeleton is done** (all 9 vendors emit LIVE end-to-end into `THIRD_PARTY_RECON_OUTPUT_PROD`, no snapshot fallback). What remains is **vendor-by-vendor business-logic fine-tuning** — call it the last 15%.

## Ground truth

- **Repo:** `C:\Users\Nate.Fold\projects\PROJECTS\Third_Party_Reconciliation\Combined_Recon_Prod_Pipeline`
- **GitHub:** https://github.com/NateFold28/Third_Party_Reconciliation_Production
- **Snowflake:** role=`DEVELOPER`, warehouse=`REPORTING_WH`, database=`ANALYTICS_DEV`, schema=`DBT_NFOLD_TRANSFORMATION`
- **Python:** `C:\Users\Nate.Fold\projects\.venv\Scripts\python.exe`
- **Connection helper:** `TEMPLATES.Python.connection.get_snowflake_connection(role, warehouse, database, schema)` (kwargs-only)

## Verified current state (2026-08-23)

- `THIRD_PARTY_RECON_OUTPUT_PROD` = **101,938 rows / 9 vendors LIVE / 12 EXCEPTION_TYPE buckets / 45 cols**
- Schema matches app expectations exactly. All 12 buckets are valid.
- **Zero** references to legacy tables (`_LEGACY_20260823`, `_SNAPSHOT_20260823`, `THIRD_PARTY_STANDALONE_RECON_DETAIL__`) in any vendor SQL. Verified by `scripts/_audit_architecture.py`.
- `VENDOR_FALLBACK = {}` in `scripts/_run_skeleton_pipeline.py` — no silent snapshot fallback allowed.
- Latest commits: `732ef9e` (unified maps), `0739570` (all 9 LIVE), `8e9ca2d` (KeepIT dedup fix + audits).

### Per-vendor clear rate + exception $ impact (current baseline)

| Vendor | Rows | Clear % | Exception $ | Top exception bucket |
|---|---:|---:|---:|---|
| Proofpoint | 5,459 | 96.1% | $1.4M | Vendor Billing/No CW Billing |
| Bitdefender | 3,385 | 86.7% | $1.8M | CW SKU/No Vendor SKU |
| Acronis | 17,634 | 82.5% | $3.8M | (multiple, see diag) |
| SentinelOne | 19,661 | 78.4% | $28.5M | API Usage Recorded No CW Billing |
| Exium | 791 | 71.6% | $0.5M | Vendor SKU/No CW SKU |
| Auvik | 3,503 | 61.7% | $3.2M | CW SKU/No Vendor SKU |
| Webroot | 16,246 | 42.0% | $2.6M | Vendor Billing Insufficient CW Billing |
| KeepIT | 21,787 | 28.4% | $6.0M | Unmapped Partner (was $182M pre-fix) |
| ESET | 13,472 | 9.3% | $0 | (zero-dollar cluster - data flow issue) |

### CW-side SKU coverage gap (biggest fine-tuning lever)

Prioritized by unmapped annual ARR from `scripts/_audit_cw_sku_universe.py`:

| Vendor | Unmapped SKUs | Unmapped Annual ARR |
|---|---:|---:|
| **Auvik** | 284 | **$30.0M** |
| **Bitdefender** | 170 | **$14.9M** |
| **SentinelOne** | 44 | **$8.6M** |
| ESET | 2,903 | $1.9M |
| Acronis | 354 | $1.0M |
| Proofpoint | 48 | $11K |

Top unmapped SKUs (dollar impact) are captured in `logs/audit_cw_sku_universe.txt`. User quote: *"the things we absolutely need to iron out are the sku and partner mappings and then the recon will likely self correct, then we can fine tune."* So SKU/partner map work is the immediate priority.

## Architecture (do not deviate)

```
ingestion (Python) -> <VENDOR>_USAGE + THIRD_PARTY_RECON_VENDOR_USAGE_PROD
                    -> sql/01 unified billing sources (Zuora / Marketplace / TRT)
                    -> sql/02 unified reference maps (RECON_PARTNER_MAP, RECON_SKU_MAP)
                    -> sql/03 compat views (bridge dead objects to unified schema)
                    -> Vendor_Recon_Pipelines_Prod/<V>/<V>_Reconciliation_Script_Prod.sql
                    -> <V>_RECON_DETAIL
                    -> _run_skeleton_pipeline.py live_emit_block()
                    -> THIRD_PARTY_RECON_DETAIL_PROD (34 cols)
                    -> build_third_party_recon_output_prod.py (LOCKED classifier)
                    -> THIRD_PARTY_RECON_OUTPUT_PROD (45 cols, 12 buckets)
                    -> Streamlit app
```

## Absolute do-nots

1. **Do NOT modify `scripts/build_third_party_recon_output_prod.py`.** The classifier is locked. If a bucket looks wrong, fix the upstream vendor SQL, not the classifier.
2. **Do NOT re-introduce `VENDOR_FALLBACK` entries.** All 9 vendors must run LIVE. Any regression that would require fallback needs to be fixed, not masked.
3. **Do NOT reference `_LEGACY_20260823`, `_SNAPSHOT_20260823`, or `THIRD_PARTY_STANDALONE_RECON_DETAIL__*`** in any new SQL. These are historical only. The unified maps + compat views are the only correct sources.
4. **Do NOT wire the contract-pricing-comparison card in the app.** User quote: *"the only thing we havent really built for the app yet is the contract pricing comparison so this should just be unpopulated in the app for now which is expected."* Leave it empty.
5. **Do NOT touch `THIRD_PARTY_RECON_OUTPUT_PROD` schema.** 45 cols is the parity contract with the app.

## Runbook

```powershell
cd C:\Users\Nate.Fold\projects\PROJECTS\Third_Party_Reconciliation\Combined_Recon_Prod_Pipeline

# Full skeleton (all 9 vendors LIVE, ~3-4 min)
..\..\..\.venv\Scripts\python.exe -u scripts\_run_skeleton_pipeline.py

# Sanity/audit tools
..\..\..\.venv\Scripts\python.exe -u scripts\_audit_architecture.py
..\..\..\.venv\Scripts\python.exe -u scripts\_diag_exception_buckets.py
..\..\..\.venv\Scripts\python.exe -u scripts\_audit_cw_sku_universe.py

# Streamlit app
run_app.bat
```

## Recommended attack order for the remaining 15%

1. **Auvik SKU map ($30M gap, 284 unmapped)** — the ConnectWise-RMM Network Monitoring SKU family (CMS-UMM-SAAS-RMM-UMM-SRMMANM, CULCSAS100710001A250, etc.) is completely unmapped. Add rows to `RECON_MANUAL_SEED_SKU_MAP` for the Auvik vendor, then re-run `sql/02_unified_reference_maps.sql` and the skeleton. Expect Auvik clear rate to jump.

2. **Bitdefender SKU map ($15M gap, 170 unmapped)** — ATS & EDR SKU family (DL17107A00*) plus Bitdefender Secure Extra/Plus, PHASR, Cloud Encryption, Patch Management. Same procedure. Bitdefender clear rate is already 86.7%, so this cleanup mostly reduces the "CW SKU/No Vendor SKU" bucket.

3. **SentinelOne SKU map + API-usage bucket** — clear rate is 78.4% but $28.5M sits in "API Usage Recorded No CW Billing". Two workstreams: (a) close the 44 unmapped CW SKUs (CW MDR Premium at $6.5M alone), (b) review the TRT/API-usage-to-billing rule in `SentinelOne_Reconciliation_Script_Prod.sql` — likely a legit-vs-suspicious threshold to tune.

4. **KeepIT partner map** — the QUALIFY dedupe recovered $178M of phantom fanout, but "Unmapped Partner" is still the top bucket at $6M and clear rate is 28.4%. Audit `RECON_PARTNER_MAP` for KeepIT vendor and the partner names showing up in KeepIT exceptions.

5. **Webroot** — 42% clear rate, top bucket is "Vendor Billing Insufficient CW Billing" ($799K in). SKU map is complete (0 gap) so this is a rate/pricing calibration.

6. **ESET zero-dollar cluster** — 9.3% clear rate but $0 exception dollars means the entire exception mass sits in rows that have no vendor_amount attached. Likely a joining/rate-population defect in `ESET_Reconciliation_Script_Prod.sql`. Diagnose before touching maps.

## Files and locations you will need

- **Vendor SQL** — `Vendor_Recon_Pipelines_Prod/<VENDOR>/<VENDOR>_Reconciliation_Script_Prod.sql` (one per vendor). Restore anchor: the pattern KeepIT used (QUALIFY on partner_bridge) can be copied to any other vendor showing partner fanout.
- **Manual seed maps** — `RECON_MANUAL_SEED_PARTNER_MAP`, `RECON_MANUAL_SEED_SKU_MAP` (Snowflake tables). These get UNIONed into `RECON_PARTNER_MAP` and `RECON_SKU_MAP` by `sql/02_unified_reference_maps.sql`.
- **CW SKU catalog** — `analytics.dbo_transformation.seed__product_categorization` joined to `analytics.dbo.carr__all_transactions` for revenue-weighted prioritization. The audit script already does this.
- **Compat views** — `sql/03_compat_dead_object_views.sql`. If a vendor SQL references an object that doesn't exist, either update the vendor SQL to point at the unified schema, or add a view here.
- **App** — `app/combined_recon_app.py`. Locked schema contract. Contract pricing comparison card intentionally empty for now.

## Snowflake gotcha (bit me twice)

Correlated `NOT IN (SELECT ...)` subqueries throw `Unsupported subquery type cannot be evaluated`. Always rewrite as `LEFT JOIN ... WHERE x IS NULL`. See `scripts/_audit_cw_sku_universe.py` for the pattern.

## First action for the new session

1. Read this file end-to-end.
2. Read the current README at `PROJECTS/Third_Party_Reconciliation/Combined_Recon_Prod_Pipeline/README.md`.
3. Read `logs/audit_cw_sku_universe.txt` (top-10 unmapped SKUs per vendor with dollar impact).
4. Ask the user: *"Attack the Auvik SKU map first ($30M gap) or a different vendor?"* — then start editing `RECON_MANUAL_SEED_SKU_MAP` accordingly.

## Session memory to check first thing

- `/memories/repo/production_consolidation_complete_2026_08_20.md`
- `/memories/repo/unified_recon_schema_2026_08_19.md`
- `/memories/repo/pipeline_architecture_tables_2026_08_20.md`
- `/memories/repo/hardcoding_audit_completion_2026_08_20.md`
