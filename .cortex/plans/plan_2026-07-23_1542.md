---
created: 2026-07-23T15:42:42.883Z
session: 54705a9c-4b10-485f-ab9e-69f66bbcc69e
working_directory: C:\Users\Nate.Fold\projects
---

## What's Actually Happening

Marcel's concerns and your pipeline state are two separate issues that are getting confused:

### Issue 1: Marcel's Seed File Quality Concerns (Valid but Already Handled)

Marcel found in your xlsx file:
- **487 exact duplicates** (same partner_name + same SF_ID repeated)
- **39 conflicting names** (same partner_name → different SF_IDs, often M&A scenarios)
- **Some wrong mappings** (CMIT Solutions of Cincinnati East → wrong account)

**These issues already exist in the current V3 table in Snowflake** (10,371 rows). They're not new bugs you introduced — they're inherited from the manual team's original mapping work. Your pipeline SQL already handles them with `QUALIFY ROW_NUMBER()` dedup logic that picks one deterministic winner per name.

### Issue 2: The 6 "Unmapped" Names Marcel Listed

Marcel showed these as still unmapped:
```
DCG Technical Solutions, LLC.
onPlatinum
SAX Technology Advisors
SYST99 -  Systems Engineering
PC Integrated Solutions
Global Data Systems Inc
```

**These ALL resolve correctly with your fuzzy matching SQL** (already on disk in `01_proofpoint_vendor.sql`). I just confirmed: the normalization strips punctuation and collapses whitespace, so "DCG Technical Solutions, LLC." matches "DCG Technical Solutions, LLC" in V3. Marcel may be testing against the raw seed file directly rather than through the pipeline SQL.

### Issue 3: The 62 Additions Haven't Been Loaded Yet

The V3 table in Snowflake still has 10,371 rows — the 62 new mappings (TTx sub-customers, OT Group Managed Services, etc.) are NOT in there yet. They exist in `PROOFPOINT_PARTNER_ADDITIONS` as a staging table but haven't been merged into V3.

### What Needs to Happen

1. **Marcel should deduplicate the xlsx** before loading (remove exact dupes, pick one SF_ID for conflicts) — this is a data hygiene task, not a pipeline logic issue
2. **The 62 additions need to be in the final file** — confirm they're in the xlsx at `C:\Users\Nate.Fold\OneDrive - ConnectWise, Inc\Proofpoint POC\Mapping Files`
3. **Once loaded to V3**, re-run `02_run_recon.py` — the fuzzy SQL + OEM fix + OVERAGE logic will produce the 98% parity result
4. **The pipeline SQL is correct** — it handles duplicates, conflicts, and punctuation variants at query time regardless of seed file messiness

### Verification Step
After Marcel loads the clean seed, run this to confirm parity:
```sql
-- Should show 0 unmapped rows
SELECT COUNT(*) FROM PROOFPOINT_VENDOR_MATCHED WHERE final_sf_id IS NULL;
```

### Critical Files
- `Proofpoint\snowflake\01_proofpoint_vendor.sql` — fuzzy matching + dedup logic (already correct on disk)
- `Proofpoint\snowflake\02_zuora_billing.sql` — OEM billing fix (already correct on disk)
- `Proofpoint\snowflake\03_reconciliation.sql` — directional OVERAGE logic (already correct on disk)
- `PROOFPOINT_PARTNER_ADDITIONS` table — the 62 rows that must be in the final seed file
- The xlsx at Mapping Files folder — needs dedup before engineering loads it